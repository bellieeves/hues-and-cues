{
  "name": "hues-and-clues",
  "version": "1.0.0",
  "private": true,
  "description": "Multiplayer ESL color-guessing game (Node + WebSocket backend, React frontend).",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "node server/server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=20"
  }
}
